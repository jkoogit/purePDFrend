# 10-12. DB 장애 대응 처리 정책 구현 및 AGENTS.md v2.0 거버넌스 승급 리뷰

> **작성일**: 2026-09-20  
> **작성자**: gemini (AI Agent)  
> **태스크 ID**: `TASK-20260920-004`  
> **세션 ID**: `SESSION-20260917-001`  
> **이슈 번호**: Issue #5  
> **상태**: 승급 완료 및 종결 (Promoted & Done)

---

## 🔍 1. 구현 개요 및 목적 (Executive Summary)

1. **DB 장애 상태 대응 5대 운영 정책(정책 2.1 ~ 2.5) 수립 및 UI 전면 적용**:
   - **정책 2.1 (조회 전용)**: 에이전트 서비스 화면은 기본 조회 모드로 작동하여 임의 변형을 방지.
   - **정책 2.2 (조회 결과 없음 안내)**: DB 연결이 원활하지 않거나 데이터가 없을 때, `AgentUsageViewer`, `TaskInfoManager`, `WorkGraphViewer`, `DocsGovernanceManager` 전체 데이터 영역에 `"조회된 결과가 없습니다."` 문구와 함께 상태 점검 안내 노출.
   - **정책 2.3 (수정 이벤트 발생 시 경고 방어)**: 뷰 설정 저장, 문서 동기화, 고아 문서 정리, OCR 설정 저장 등 영속화 액션 발생 시 DB 미연결 상태일 경우 `"영속화 상태 점검필요"` 경고 메시지를 명확히 출력하고 작업을 안전하게 방어.
   - **정책 2.4 (PDF 비즈니스 서비스 오프라인 독립 실행)**: 순수 PDF 변환/뷰어/OCR 처리 등 핵심 기능은 DB 연결 상태와 무관하게 로컬 오프라인 모드로 독립적이고 완벽하게 동작하도록 보장.
   - **정책 2.5 (먹통/Freezing 방지)**: DB 브릿지 단절, 타임아웃 발생 시에도 프론트엔드가 멈추거나 먹통이 되지 않고 비동기 캐시 및 로컬 스토어로 원활하게 폴백.
2. **`AGENTS.md v2.0` 거버넌스 규약 제정 및 시스템 강화**:
   - 채팅 시작 시 `[0000]` 형식의 채팅 번호와 함께 사전 계획 보고 의무화.
   - `#태스크시작` 단독 인입 시 Read-Only 분석 모드 강제 (코드/문서 수정 원천 차단).
   - `#태스크처리` 명시 시에만 TDD/DDD 7단계 파이프라인 기반 코드 구현 및 린트/컴파일 검증 수행.
   - `#태스크정리, #태스크승급` 단계에서 신규 업무 프롬프트 유입 시 백로그 분기 질의 피드백 원칙 정립.
3. **거버넌스 정책 문서 03-10 신설**:
   - `docs/03.정책/03-10_DB장애대응_및_무중단운영_정책.md` 발행 및 인덱스 현행화.

---

## 📐 2. 보완턴 6단계 점검 (Refinement Turn Evaluation)

| 단계 | 항목 | 점검 결과 | 상세 내용 |
| :---: | :--- | :---: | :--- |
| **Step 1** | 테스트 코드 및 검증 | **완료** | `tsc --noEmit` 정적 린트 통과 및 `npm run build` 번들 컴파일 성공 검증 |
| **Step 2** | DDD 원칙 준수 | **완료** | 하네스 생명주기 및 DB 연결 상태(Connected/Fallback) 도메인 상태 분리 및 격리 |
| **Step 3** | 아키텍처 점검 | **완료** | UI 전역에 `dbStatus` 파라미터 계층 전파 및 영속화 가드 도입 |
| **Step 4** | 보안 및 거버넌스 | **완료** | 정책 03-10 제정 및 AGENTS.md v2.0 규제 게이트 수립 |
| **Step 5** | 성능 및 내구성 | **완료** | Zero-Hang 원칙 준수: DB 미연결 시 로컬 캐시 폴백으로 화면 먹통 방지 |
| **Step 6** | 문서화 및 동기화 | **완료** | 03-10 정책 문서 발행, 10-12 코드리뷰 발행, 하네스 스토어 상태 승급 완료 |

---

## 📊 3. 최종 상태 승급 및 검증 메트릭

```mermaid
graph LR
    Task[TASK-20260920-004] --> Loop10[LOOP-010: DB장애 정책 2.1~2.5 UI/UX 반영]
    Task --> Loop11[LOOP-011: AGENTS.md v2.0 거버넌스 및 정책 03-10 수립]
    Loop10 --> Done10[상태: 완료]
    Loop11 --> Done11[상태: 완료]
    Task --> TaskDone[상태: 승급 완료 및 종결 (Promoted & Done)]
```

- **하네스 메타데이터 상태**:
  - `TASK-20260920-004`: `완료`
  - `LOOP-20260920-010`: `완료`
  - `LOOP-20260920-011`: `완료`
  - `SESSION-20260917-001`: `완료`
