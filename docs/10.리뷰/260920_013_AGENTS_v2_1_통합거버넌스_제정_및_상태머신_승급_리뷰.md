# 10-13. AGENTS.md v2.1 통합 거버넌스 규약 제정 및 상태머신 수립 리뷰

> **작성일**: 2026-09-20  
> **작성자**: gemini (AI Agent)  
> **태스크 ID**: `TASK-20260920-005`  
> **세션 ID**: `SESSION-20260917-001`  
> **이슈 번호**: Issue #6  
> **상태**: 승급 완료 및 종결 (Promoted & Done)

---

## 🔍 1. 구현 개요 및 목적 (Executive Summary)

1. **`AGENTS.md v2.1` 통합 거버넌스 규약 전면 개정**:
   - **하네스 생명주기 및 상태 전이 다이어그램 (Mermaid) 구축**: 세션 ➔ 태스크 ➔ 루프의 3계층 상태머신을 최상단에 배치하여 AI 에이전트의 상태 오판을 원천 방지.
   - **대화 턴 추적 및 영속화 기술적 주체 명확화**: `TokenQuotaDetectionService` 및 `POST /api/agent/trace/turn` API를 통해 토큰 한도 초과(429, RESOURCE_EXHAUSTED) 오류 턴을 자동 필터링한 후 `aiagent.agent_conversation_trace` 및 로컬 스토어에 안전하게 영속화하도록 주체 명시.
   - **원격 Git 제어 경로 공식화 (GitHub REST API)**: 로컬 `.git` 및 `gh` CLI 부재 샌드박스 환경에서 `GITHUB_TOKEN`을 이용한 GitHub REST API 직접 호출 원칙 수립.
   - **채팅 번호 표기 규칙 정립**: 대단위 세션/회고는 `[0000]`, 세션 내 마이크로 태스크 턴은 `[00]`으로 분리하여 빠른 호흡 유도.
   - **READ-ONLY 게이트 범위의 논리적 무결성 확보**: `#태스크시작`은 완전한 READ-ONLY, `#태스크정리`는 소스 수정 금지 및 리뷰/스토어 동기화 허용, `#태스크승급`은 상태값 '완료' 수정 전용으로 명확화.
2. **거버넌스 정책 03-11 제정**:
   - `docs/03.정책/03-11_에이전트_통합거버넌스_및_실행규제_정책.md` 신설 및 인덱스 현행화 완료.

---

## 📐 2. 보완턴 6단계 점검 (Refinement Turn Evaluation)

| 단계 | 항목 | 점검 결과 | 상세 내용 |
| :---: | :--- | :---: | :--- |
| **Step 1** | 테스트 코드 및 검증 | **완료** | `tsc --noEmit` 정적 린트 통과 및 `npm run build` 번들 컴파일 성공 검증 |
| **Step 2** | DDD 원칙 준수 | **완료** | 하네스 생명주기(세션-태스크-루프) 및 토큰 소진 감지 도메인 서비스 파사드 연계 무결성 확보 |
| **Step 3** | 아키텍처 점검 | **완료** | GitHub REST API 기반 샌드박스 원격 제어 경로 정립으로 인프라 제약 극복 |
| **Step 4** | 보안 및 거버넌스 | **완료** | `AGENTS.md v2.1` 행동 강령 및 `03-11` 거버넌스 정책 문서 완비 |
| **Step 5** | 성능 및 내구성 | **완료** | 태스크 마이크로 턴 유도(`[00]`) 및 Zero-Hang 정책 연동 |
| **Step 6** | 문서화 및 동기화 | **완료** | 03-11 정책 문서, 10-13 코드리뷰 발행 및 README 인덱스, 하네스 스토어 상태 승급 완료 |

---

## 📊 3. 최종 상태 승급 및 검증 메트릭

```mermaid
graph LR
    Task[TASK-20260920-005] --> Loop12[LOOP-012: AGENTS.md v2.1 규약 제정 및 03-11 정책 수립]
    Loop12 --> Done12[상태: 완료]
    Task --> TaskDone[상태: 승급 완료 및 종결 (Promoted & Done)]
```

- **하네스 메타데이터 상태**:
  - `TASK-20260920-005`: `완료`
  - `LOOP-20260920-012`: `완료`
  - `SESSION-20260917-001`: `완료`
- **원격 브랜치 배포 승급 결과 (GitHub REST API)**:
  - `dev` (`cdb1108`) ➔ `stg` (`174a54f`): 배포 머지 완료
  - `stg` (`174a54f`) ➔ `main` (`7356901`): 배포 머지 완료 (stg, main 최신화 달성)
